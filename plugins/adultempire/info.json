{
  "name": "adultempire",
  "version": "0.7.0",
  "authors": ["boi123212321"],
  "description": "Scrape data from adultempire",
  "events": ["movieCreated", "actorCreated"],
  "arguments": [
    {
      "name": "dry",
      "type": "Boolean",
      "required": false,
      "default": false,
      "description": "Whether to commit data changes"
    },
    {
      "name": "fuzzyActorCheck",
      "type": "Boolean",
      "required": false,
      "default": false,
      "description": "If true, the plugin will confirm that the adultempire's actor found (or one of its aliases) actually matches the searched actor name (with some tolerance using the Levenshtein algorithm). If there is no match, an error will be returned and the actor will not be scraped from an incorrect match."
    }
  ]
}
